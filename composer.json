{
    "require-dev": {
        "humanmade/coding-standards": "^1.1"
    },
    "config": {
        "allow-plugins": {
            "dealerdirect/phpcodesniffer-composer-installer": true
        }
    },
    "scripts": {
        "format": "vendor/bin/phpcbf --standard=phpcs.ruleset.xml -s .",
        "lint": "./vendor/bin/phpcs --standard=phpcs.ruleset.xml -s ."
      }
}
